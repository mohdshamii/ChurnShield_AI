name: base
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - pytest
    - flake8
    # Add your other Python packages here
    # Example:
    # - pandas
    # - scikit-learn
    # - numpy
